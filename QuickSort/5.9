quicksort的本质是分治，基本思路如下：（实现将小于等于X的数放到左边，而右边全是大于等于X的数）
1.确定分界点
2.调整范围
3.递归处理左右两端
延伸：实验报告6的第五条，奇偶数分类，也是利用的这个思想。
方法一：O(n),使用额外空间暴力解题
1.a[],b[]（2个新数组）
2.q[l-r]中，若q[i]<=x,q[i]放入a数组，否则放入b数组
3.a、b数组依次放入q中
方法二：双指针法
左右两个指针依次向里面移动，比较交换
今天学到了：左移一位相当于除以2，但是运行的效率和速度都更高了
理想化的实现过程是用BufferReader,but I haven't learned it before,so I will use Scanner instead;
代码实现如下：
